

namespace DeltaSpecialForce3D.Enums
{
    public enum GameMode { None, TeamDeathmatch, ZombieSurvival }
    public enum GameState { Setup, Countdown, RoundActive, RoundEnd, MatchEnd }
    public enum GameResult { None, Win, Lose }
    public enum ShopState { SelectType, SelectWeapon }
}
